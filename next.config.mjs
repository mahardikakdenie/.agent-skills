/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['friendsure-spaces.sgp1.digitaloceanspaces.com', 'placehold.co', 'storage.googleapis.com', 'cdn-stg.friendsureapp.com', 'cdn-stg.frndsr.tech'],
    },
  };
  
  export default nextConfig;
  