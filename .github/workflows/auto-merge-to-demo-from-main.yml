on:
  push:
    branches:
      - main
permissions:
  contents: write
  pull-requests: write
  
jobs:
  auto-merge:
    uses: friendsure/.github/.github/workflows/auto-merge-from-source-to-target.yml@main
    with:
      source_branch: main
      target_branch: demo
