# Legacy Update - admin-portal - 2026-02-15 01:07:41 +07:00

## Update Metadata

- **Integrated commit (integrate/admin-portal):** `356c77e88d40b3739faa654997df46da0d3473ab`
- **Legacy head commit merged from remote (`admin-portal/stage`):** `ac46d418824310c13812785c42e16d1f499435c5`
- **Recent legacy commits included in this update:**
  - `0fa4d4790` fix: convert empty strings to null for new email template creation.
  - `1fb2956c5` feat(renewal): AA-12 Implement pending renewals page.
  - `5aacca186` fix: Use `status` property instead of `table_status` for policy notification log display.
  - `5051b0518` fix: Display history item name as a fallback when its status is falsy.
  - `61ee82398` Refactor: Update status display to only show `h?.status` by removing the `h?.name` fallback.
  - `ac46d4188` feat(claim): GE-28 claim export columns and data based on claim configuration.

## Files Changed from Legacy Update

- `apps/admin-portal/src/app/claim/list/page.tsx`
- `apps/admin-portal/src/app/policy/pending-renewals/page.tsx`
- `apps/admin-portal/src/components/tableConfig/policyTableConfig.tsx`
- `apps/admin-portal/src/components/ui/DataTable/index.tsx`
- `apps/admin-portal/src/constants/app-menu.const.tsx`
- `apps/admin-portal/src/constants/app-url.const.tsx`
- `apps/admin-portal/src/hooks/useEmailTemplateForm.hooks.tsx`
- `apps/admin-portal/src/hooks/useExportClaim.hooks.tsx`
- `apps/admin-portal/src/hooks/useNotificationLogs.hooks.tsx`
- `apps/admin-portal/src/hooks/usePolicies.hooks.tsx`
- `apps/admin-portal/src/services/report.service.ts`

## Change Analysis

### 1. New service domain / new base URL

- `src/services/report.service.ts` introduces:
  - Base URL: `NEXT_PUBLIC_REPORT_SERVICE_URL`
  - New endpoints:
    - `GET /v1/notification-logs`
    - `GET /v1/notification-logs/:id`
  - New token dependency: `NEXT_PUBLIC_REPORT_SERVICE_TOKEN`
- `src/hooks/useNotificationLogs.hooks.tsx` introduces new API usage for notification logs.
- **Impact:** `High`
  - New external service dependency not yet represented in migration service docs.
  - Runtime failure risk if env vars are missing.
- **Decision Matrix mapping:** New service (new base URL) -> **Batch 5**

### 2. Existing endpoint behavior update (Policy Service)

- `src/hooks/usePolicies.hooks.tsx` adds `is_need_renewal=true` flag for pending-renewal use case while still calling existing policy list endpoint.
- No new base URL; existing policy service is reused.
- **Impact:** `Medium`
  - Behavior/filtering change for policy list retrieval.
- **Decision Matrix mapping:** Modified existing endpoint usage -> **Batch 4**

### 3. Component-only/UI updates

- New page: `src/app/policy/pending-renewals/page.tsx`
- New table config: `src/components/tableConfig/policyTableConfig.tsx` (pending renewals columns)
- Route/menu additions:
  - `src/constants/app-url.const.tsx`
  - `src/constants/app-menu.const.tsx`
- DataTable row key changed from `item.id || index` to `index` in `src/components/ui/DataTable/index.tsx`
- **Impact:** `Medium`
  - Feature addition is straightforward.
  - Row key change may affect row identity stability during pagination/sorting.
- **Decision Matrix mapping:** Component-only changes -> **Batch 6** (or Batch 4 minimal if adjacent service adjustment needed)

### 4. Business logic update (Claim export)

- `src/hooks/useExportClaim.hooks.tsx` changes export columns for Grab Express:
  - From fixed columns (`Booking ID`, `Tanggal Claim`) to dynamic columns from `claim_config`.
  - Excludes config types `multiple file` and `file`.
- **Impact:** `Medium-High`
  - Export schema/column naming changed; downstream report consumers may be affected.
- **Decision Matrix mapping:** Modified existing behavior (no new base URL) -> **Batch 4**

### 5. Payload normalization update (Email template)

- `src/hooks/useEmailTemplateForm.hooks.tsx` now normalizes empty string values to `null` for create as well as edit flow.
- **Impact:** `Low-Medium`
  - API payload contract behavior adjusted; typically safe but should be validated.
- **Decision Matrix mapping:** Modified existing behavior -> **Batch 4**

## Base URL Cross-Check (audit.md and plan.md)

- `NEXT_PUBLIC_REPORT_SERVICE_URL` is **not present** in:
  - `apps/admin-portal/docs/migration/service/audit.md`
  - `apps/admin-portal/docs/migration/service/plan.md`
- Existing mapped service base URLs remain unchanged.

## New Types/Interfaces Identified

- In `src/services/report.service.ts`:
  - `NotificationLog`
  - `NotificationLogsResponse`
  - `GetNotificationLogsParams`

## Breaking Change Assessment

- **Potential breaking operational change:** new required env vars (`NEXT_PUBLIC_REPORT_SERVICE_URL`, `NEXT_PUBLIC_REPORT_SERVICE_TOKEN`).
- **Potential consumer-level breaking change:** claim export column shape/name changes for Grab Express.
- **No hard API signature break detected** in existing service classes from this diff set.

## Current Refactor Phase

- **Status:** `Unknown/Not explicitly documented`
- `apps/admin-portal/docs/migration/service/task.md` not found, so current main batch cannot be confirmed from task tracking.

## Recommended Next Batch

- **Primary recommendation:** **Batch 5** (`Apply Adjustments - New Service Required`)
- **Justification:**
  - New service domain/base URL detected (`NEXT_PUBLIC_REPORT_SERVICE_URL`) and missing from audit/plan docs.
  - This matches Batch 5 trigger criteria in decision matrix (new base URL not in audit).
- **Additional note for execution:**
  - While running Batch 5 for report service, include minimal Batch 4-style updates for existing behavior changes (policy renewal filter, claim export logic validation, email template payload normalization), then proceed to Batch 6 verification.
