# Admin Portal Parity Checklist (Batch 1)

Shared-candidate components: **34**

## Alert (`src/components/ui/alert.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Badge (`src/components/ui/badge.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Breadcrumb (`src/components/ui/breadcrumb.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Button (`src/components/ui/button.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Calendar (`src/components/ui/calendar.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Card (`src/components/ui/card.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Checkbox (`src/components/ui/checkbox.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Combobox (`src/components/ui/combobox.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Command (`src/components/ui/command.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## DateRangePicker (`src/components/ui/date-range-picker.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Dialog (`src/components/ui/dialog.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Drewer (`src/components/ui/drewer.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## DropdownMenu (`src/components/ui/dropdown-menu.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## SelectAutocomplete (`src/components/ui/Fields/SelectAutocomplete/index.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## UploadFile (`src/components/ui/Fields/UploadFile/index.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Form (`src/components/ui/form.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## ImageOrDefault (`src/components/ui/image-or-default.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Input (`src/components/ui/input.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## JourneyVerticalImage (`src/components/ui/journey-vertical.image.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Label (`src/components/ui/label.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Loading (`src/components/ui/loading.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Loading (`src/components/ui/Loading/index.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Menubar (`src/components/ui/menubar.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## NavigationMenu (`src/components/ui/navigation-menu.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Pagination (`src/components/ui/pagination.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Popover (`src/components/ui/popover.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## RadioGroup (`src/components/ui/radio-group.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Select (`src/components/ui/select.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Spinner (`src/components/ui/spinner.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Switch (`src/components/ui/switch.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Table (`src/components/ui/table.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Tabs (`src/components/ui/tabs.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Textarea (`src/components/ui/textarea.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

## Tooltip (`src/components/ui/tooltip.tsx`)

- [ ] Interaction parity (click, hover, keyboard)
- [ ] Visual parity (layout, spacing, color)
- [ ] State parity (loading, error, disabled, empty)
- [ ] Prop API still compiles after swap

